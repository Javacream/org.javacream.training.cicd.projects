pipeline {
    agent {
        docker {
            label 'docker'
            image 'maven'
            }
        }
    stages {
        stage('check maven is available') {
            steps {
                sh 'mvn -v'
            }
        }
    }
}