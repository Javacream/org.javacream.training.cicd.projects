pipeline {
    agent any

    stages {
        stage('Hello') {
            messages = "Hello Jenkins File Hieber in VSC via VAR"
            steps {
                echo '${message}'
            }
        }
    }
}
